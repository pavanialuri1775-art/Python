#list:Lists are used to store multiple items in a single variable
#Lists are created using square brackets

#example
thelist=["apple","banana","cherry"]
print(thelist)

#list items are ordered,mutable and allows duplicate values
#List items are indexed and you can access them by referring to the index number
#example
thelist=["apple","banana","cherry"]
print(thelist[0])
print(thelist[1])

#negative index means starts from the end
thelist=["apple","banana","cherry"]
print(thelist[-1])

#range of indexes
thislist = ["apple", "banana", "cherry", "orange", "kiwi", "melon", "mango"]
print(thislist[2:5])

#negative ranging of indexes
thislist = ["apple", "banana", "cherry", "orange", "kiwi", "melon", "mango"]
print(thislist[-4:-1])

#in :To determine if a specified item is present in a list use the in keyword
thelist=["apple","banana","cherry"]
if "apple" in thelist:
    print(True)













